stages:
- name: 构建并发布镜像
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: hsw212321/opcua-serverr:v0.1
      registry: 192.168.137.100
- name: 部署OPC UA服务
  steps:
  - applyYamlConfig:
      path: ./opcua-server.yml
timeout: 60
notification: {}
